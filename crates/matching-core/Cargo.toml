[package]
name = "matching-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rust_decimal = { version = "1", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
bincode = "1"

[dev-dependencies]
rust_decimal_macros = "1"
